/**
 * 两数相加
 * @param a
 * @param b
 * @returns
 */
export const add = (a: number, b: number) => {
    return a + b;
};

/**
 * 两数相减
 * @param a
 * @param b
 * @returns
 */
export const reduce = (a: number, b: number) => {
    return a - b;
};
